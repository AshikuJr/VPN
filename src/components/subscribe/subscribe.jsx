import React from 'react';
import './subscribe.sass';

const Subscribe = props => {
  return <div className="subscribe">
    <h2 className="subscribe__title">Subscribe Now for Special Features!</h2>
    <button className="subscribe__button">Subscribe Now</button>
    <span className="subscribe__subtitle">Let's subscribe with us and find the fun.</span>
  </div>
};

export default Subscribe;